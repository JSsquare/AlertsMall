<%- model_class = Tweet -%>
<div class="page-header">
  <h1 style="text-align: center; font-family: sans-serif;">Tweet for @AlertsMall</h1>
  <!--<h1><%=t '.title', :default => [:'helpers.titles.new', '%{model} For @PuneFoodAlerts'], :model => model_class.model_name.human.titleize %></h1>-->
</div>

<%= form_for @tweet, :html => { :class => 'form-horizontal' } do |f| %>
    <div class="control-group" style="text-align: center;">
      <div id="inner" style="display: inline-block;">
      <%= f.label 'tweet', :class => 'label-for-tweet'  %>
        <%= f.text_field :body, :class => 'text_field' %>
        <%= f.submit 'Tweet', :class => 'btn primary-btn tweet-action tweet-btn js-tweet-btn' %>
        <!--  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                          tweets_path, :class => 'btn' %>-->
      </div>
    </div>

    <div class="form-actions">

    </div>
<% end %>